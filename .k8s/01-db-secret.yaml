apiVersion: v1
kind: Secret
metadata:
  name: db-secret # 이 이름으로 다른 컨테이너들이 DB 정보를 가져다 씁니다.
type: Opaque
stringData:
  # [자동 치환 대상] UserData가 실제 RDS 주소로 바꿀 예정입니다.
  DB_HOST: "RDS_ENDPOINT_PLACEHOLDER"
  DB_USER: "user01"
  DB_PASS: "user01password"
  DB_NAME: "test_db"